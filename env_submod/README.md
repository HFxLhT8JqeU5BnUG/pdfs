Just a way to centralize getting env variables. 

To add this as a submodule to a project:

```git submodule add https://github.com/HFxLhT8JqeU5BnUG/env_submod.git env_submod```

Dependencies:
    
    python-dotenv